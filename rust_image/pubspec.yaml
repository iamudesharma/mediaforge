name: rust_image
resolution: workspace
description: >
  Flutter image editor plugin powered by Rust — re-exports rust_image_editor
  for backward compatibility. Prefer depending on rust_image_editor directly.
version: 0.1.0
homepage: https://github.com/iamudesharma/rust_image
repository: https://github.com/iamudesharma/rust_image

environment:
  sdk: ^3.12.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  rust_image_editor:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter:
  # Assets and editor UI live in rust_image_editor (P0.4).
