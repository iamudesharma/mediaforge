// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'image.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `hash`

/// Build feathered skin mask at edit resolution from native analysis.
SegmentationMask buildSkinMaskFromAnalysis({
  required FaceAnalysisResult analysis,
  required int width,
  required int height,
}) => RustLib.instance.api.crateApiFaceBuildSkinMaskFromAnalysis(
  analysis: analysis,
  width: width,
  height: height,
);

/// Flattened mask build — avoids serializing full [FaceAnalysisResult] over FFI.
SegmentationMask buildSkinMaskFromLandmarks({
  required List<Landmark2D> landmarks,
  required int faceContourCount,
  required List<int> regionCounts,
  SegmentationMask? segmentation,
  required int width,
  required int height,
}) => RustLib.instance.api.crateApiFaceBuildSkinMaskFromLandmarks(
  landmarks: landmarks,
  faceContourCount: faceContourCount,
  regionCounts: regionCounts,
  segmentation: segmentation,
  width: width,
  height: height,
);

/// Builds an eye regional mask from face landmarks.
SegmentationMask buildEyeMaskFromLandmarks({
  required List<Landmark2D> landmarks,
  required int faceContourCount,
  required List<int> regionCounts,
  required int width,
  required int height,
}) => RustLib.instance.api.crateApiFaceBuildEyeMaskFromLandmarks(
  landmarks: landmarks,
  faceContourCount: faceContourCount,
  regionCounts: regionCounts,
  width: width,
  height: height,
);

/// Builds a lip regional mask from face landmarks.
SegmentationMask buildLipMaskFromLandmarks({
  required List<Landmark2D> landmarks,
  required int faceContourCount,
  required List<int> regionCounts,
  required int width,
  required int height,
}) => RustLib.instance.api.crateApiFaceBuildLipMaskFromLandmarks(
  landmarks: landmarks,
  faceContourCount: faceContourCount,
  regionCounts: regionCounts,
  width: width,
  height: height,
);

/// Builds a cheek blush regional mask from face landmarks.
SegmentationMask buildBlushMaskFromLandmarks({
  required List<Landmark2D> landmarks,
  required int faceContourCount,
  required List<int> regionCounts,
  required int width,
  required int height,
}) => RustLib.instance.api.crateApiFaceBuildBlushMaskFromLandmarks(
  landmarks: landmarks,
  faceContourCount: faceContourCount,
  regionCounts: regionCounts,
  width: width,
  height: height,
);

/// Builds an under-eye regional mask from face landmarks.
SegmentationMask buildUnderEyeMaskFromLandmarks({
  required List<Landmark2D> landmarks,
  required int faceContourCount,
  required List<int> regionCounts,
  required int width,
  required int height,
}) => RustLib.instance.api.crateApiFaceBuildUnderEyeMaskFromLandmarks(
  landmarks: landmarks,
  faceContourCount: faceContourCount,
  regionCounts: regionCounts,
  width: width,
  height: height,
);

/// Returns true if the FaceAnalysisResult is valid (sufficient confidence and landmark count).
bool faceAnalysisIsValid({required FaceAnalysisResult analysis}) =>
    RustLib.instance.api.crateApiFaceFaceAnalysisIsValid(analysis: analysis);

/// Returns the minimum number of landmarks required by the vision tracker.
int visionMinLandmarkCount() =>
    RustLib.instance.api.crateApiFaceVisionMinLandmarkCount();

/// CPU skin smooth for export / non-GPU preview.
RgbaImageBuffer applySkinSmoothCpu({
  required RgbaImageBuffer buffer,
  required SegmentationMask mask,
  required double strength,
}) => RustLib.instance.api.crateApiFaceApplySkinSmoothCpu(
  buffer: buffer,
  mask: mask,
  strength: strength,
);

/// Full regional beauty on still photo (Nexus B).
RgbaImageBuffer applyBeautyCpu({
  required RgbaImageBuffer buffer,
  required List<Landmark2D> landmarks,
  required int faceContourCount,
  required List<int> regionCounts,
  required SegmentationMask skinMask,
  required BeautyParams params,
  SegmentationMask? excludeMask,
}) => RustLib.instance.api.crateApiFaceApplyBeautyCpu(
  buffer: buffer,
  landmarks: landmarks,
  faceContourCount: faceContourCount,
  regionCounts: regionCounts,
  skinMask: skinMask,
  params: params,
  excludeMask: excludeMask,
);

/// Params for a one-tap beauty look (Nexus C).
BeautyParams beautyParamsForLook({required BeautyLookPreset preset}) =>
    RustLib.instance.api.crateApiFaceBeautyParamsForLook(preset: preset);

/// User-facing name for a beauty look chip.
String beautyLookDisplayName({required BeautyLookPreset preset}) =>
    RustLib.instance.api.crateApiFaceBeautyLookDisplayName(preset: preset);

/// One-tap beauty look preset (Nexus C).
enum BeautyLookPreset { natural, soft, glow, glam, clear, peach, bold }

/// Regional beauty parameters (Nexus B) — stored in edit graph.
class BeautyParams {
  /// 0–1 skin smooth on cheeks/forehead.
  final double skinSmooth;

  /// 0–1 eye luminance lift.
  final double eyeBrighten;

  /// Selected lip tint preset.
  final LipTintPreset lipTint;

  /// 0–1 lip tint strength (when lip_tint != None).
  final double lipTintStrength;

  /// 0–1 lip plump (radial warp).
  final double lipPlump;

  /// 0–1 cheek blush.
  final double blush;

  /// 0–1 under-eye softening (Nexus E).
  final double underEye;

  /// 0–1 teeth whitening (Nexus E).
  final double teethWhiten;

  /// 0–1 high-pass skin texture preserve (Glass Skin / Clean Beauty).
  final double skinPreserveDetail;

  /// 0–1 eye enlarge warp.
  final double eyeEnlarge;

  /// 0–1 jaw slim warp.
  final double jawSlim;

  /// 0–1 nose slim warp.
  final double noseSlim;

  /// 0–1 overall face slim.
  final double faceSlim;

  /// 0–1 chin V-shape warp.
  final double chinVshape;

  const BeautyParams({
    required this.skinSmooth,
    required this.eyeBrighten,
    required this.lipTint,
    required this.lipTintStrength,
    required this.lipPlump,
    required this.blush,
    required this.underEye,
    required this.teethWhiten,
    required this.skinPreserveDetail,
    required this.eyeEnlarge,
    required this.jawSlim,
    required this.noseSlim,
    required this.faceSlim,
    required this.chinVshape,
  });

  static Future<BeautyParams> default_() =>
      RustLib.instance.api.crateApiFaceBeautyParamsDefault();

  /// Checks if any beauty parameters are active.
  Future<bool> isActive() =>
      RustLib.instance.api.crateApiFaceBeautyParamsIsActive(that: this);

  @override
  int get hashCode =>
      skinSmooth.hashCode ^
      eyeBrighten.hashCode ^
      lipTint.hashCode ^
      lipTintStrength.hashCode ^
      lipPlump.hashCode ^
      blush.hashCode ^
      underEye.hashCode ^
      teethWhiten.hashCode ^
      skinPreserveDetail.hashCode ^
      eyeEnlarge.hashCode ^
      jawSlim.hashCode ^
      noseSlim.hashCode ^
      faceSlim.hashCode ^
      chinVshape.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BeautyParams &&
          runtimeType == other.runtimeType &&
          skinSmooth == other.skinSmooth &&
          eyeBrighten == other.eyeBrighten &&
          lipTint == other.lipTint &&
          lipTintStrength == other.lipTintStrength &&
          lipPlump == other.lipPlump &&
          blush == other.blush &&
          underEye == other.underEye &&
          teethWhiten == other.teethWhiten &&
          skinPreserveDetail == other.skinPreserveDetail &&
          eyeEnlarge == other.eyeEnlarge &&
          jawSlim == other.jawSlim &&
          noseSlim == other.noseSlim &&
          faceSlim == other.faceSlim &&
          chinVshape == other.chinVshape;
}

/// Output of native face pipeline (Vision or MediaPipe).
class FaceAnalysisResult {
  /// List of 2D landmark coordinates.
  final List<Landmark2D> landmarks;

  /// Confidence score (0.0 to 1.0).
  final double confidence;

  /// Optional face/selfie segmentation mask.
  final SegmentationMask? segmentation;

  /// Count of leading landmarks that belong to the face contour (Vision); 0 = legacy estimate.
  final int faceContourCount;

  /// Point counts per region after contour (Vision order); empty = built-in fallback.
  final Uint32List regionCounts;

  const FaceAnalysisResult({
    required this.landmarks,
    required this.confidence,
    this.segmentation,
    required this.faceContourCount,
    required this.regionCounts,
  });

  static Future<FaceAnalysisResult> default_() =>
      RustLib.instance.api.crateApiFaceFaceAnalysisResultDefault();

  @override
  int get hashCode =>
      landmarks.hashCode ^
      confidence.hashCode ^
      segmentation.hashCode ^
      faceContourCount.hashCode ^
      regionCounts.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FaceAnalysisResult &&
          runtimeType == other.runtimeType &&
          landmarks == other.landmarks &&
          confidence == other.confidence &&
          segmentation == other.segmentation &&
          faceContourCount == other.faceContourCount &&
          regionCounts == other.regionCounts;
}

/// Normalized 2D landmark (0–1 in image space).
class Landmark2D {
  /// Horizontal position normalized from 0.0 (left) to 1.0 (right).
  final double x;

  /// Vertical position normalized from 0.0 (top) to 1.0 (bottom).
  final double y;

  /// Depth position (Z coordinate), often 0.0 or estimated from MediaPipe.
  final double z;

  const Landmark2D({required this.x, required this.y, required this.z});

  static Future<Landmark2D> default_() =>
      RustLib.instance.api.crateApiFaceLandmark2DDefault();

  @override
  int get hashCode => x.hashCode ^ y.hashCode ^ z.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Landmark2D &&
          runtimeType == other.runtimeType &&
          x == other.x &&
          y == other.y &&
          z == other.z;
}

/// Lip color swatch for regional tint.
enum LipTintPreset {
  none,
  nude,
  rose,
  berry,
  coral,
  red;

  static Future<LipTintPreset> default_() =>
      RustLib.instance.api.crateApiFaceLipTintPresetDefault();
}

/// Selfie / face segmentation mask at edit resolution (row-major R8).
class SegmentationMask {
  /// Width of the mask in pixels.
  final int width;

  /// Height of the mask in pixels.
  final int height;

  /// Grayscale pixel values (0–255), representing feathering/opacity.
  final Uint8List pixels;

  const SegmentationMask({
    required this.width,
    required this.height,
    required this.pixels,
  });

  @override
  int get hashCode => width.hashCode ^ height.hashCode ^ pixels.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SegmentationMask &&
          runtimeType == other.runtimeType &&
          width == other.width &&
          height == other.height &&
          pixels == other.pixels;
}
