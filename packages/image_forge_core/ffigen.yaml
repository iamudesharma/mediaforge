# Run with `dart run ffigen --config ffigen.yaml`.
name: ImageForgeCoreBindings
description: |
  Bindings for `src/image_forge_core.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/image_forge_core_bindings_generated.dart'
headers:
  entry-points:
    - 'src/image_forge_core.h'
  include-directives:
    - 'src/image_forge_core.h'
comments:
  style: any
  length: full
