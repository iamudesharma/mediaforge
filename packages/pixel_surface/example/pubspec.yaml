name: pixel_surface_example
resolution: workspace
description: Demo — animated RGBA frames in GpuTextureView without image_forge.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  pixel_surface:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
