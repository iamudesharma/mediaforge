name: video_forge_example
resolution: workspace
description: Minimal FRB probe example for video_forge.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  video_forge:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
