name: video_processor_core_example
resolution: workspace
description: Minimal FRB probe example for video_processor_core.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  video_processor_core:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
