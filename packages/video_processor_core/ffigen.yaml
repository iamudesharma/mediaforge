# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterVideoProcessorBindings
description: |
  Bindings for `src/flutter_video_processor.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_video_processor_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_video_processor.h'
  include-directives:
    - 'src/flutter_video_processor.h'
comments:
  style: any
  length: full
