## 0.1.0

- Initial release: `LiveCameraService`, `CameraPermission`, `TemporalFaceSmoother`.
- Android / iOS front-camera YUV420 stream; re-exports `camera` preview types.
- Extracted from `rust_image_editor` (P0.5 / P0.6).
