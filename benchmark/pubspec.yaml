name: rust_image_benchmark_runner
resolution: workspace
description: Benchmark image_forge and video_forge Dart/FRB APIs — 10 cold iterations, CPU vs GPU.
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  flutter_rust_bridge: 2.12.0
  image_forge_editor:
    path: ../packages/image_forge_editor

dev_dependencies:
  lints: ^6.0.0

executables:
  media_forge_benchmark:
